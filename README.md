# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
## Данные и типы данных в приложении

Карточка товара

```
interface ICard {
    id: string;
    description: string;
    image: string;
    title: string;
    category: string;
    price: number | null;
}
```
Данные пользователя

```
interface ICustomer {
    payment: string;
    email: string;
    phone: string;
    address: string;
    total: number;
    items: string[];
}
```
Интерфейс для модели данных карточек товаров

```
interface ICards {
    cards: ICard[];
    preview: string | null;
}
```
Интерфейс для модели данных товаров в корзине

```
interface IBasket {
    chosencards: ICard[];
}
```
Данные товаров на главной странице сайта

```
export type TCardInfo = Pick<ICard, 'title' | 'image' | 'category' | 'price'>;
```
Данные товаров в модальном окне товара

```
export type TCardInfoFull = Pick<ICard, 'title' | 'image' | 'category' | 'price' | 'description'>;
```
Данные товаров в модальном окне корзины

```
export type TCardInfoShort = Pick<ICard, 'title' | 'price'>;
```
Данные пользователя в модальном окне выбора способа оплаты и адреса

```
export type TCustomerInfoPayment = Pick<ICustomer, 'payment' | 'address'>;
```
Данные пользователя в модальном окне заполнения номера и email

```
export type TCustomerInfoContacts = Pick<ICustomer, 'email' | 'phone'>;
```
## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP:
- слой представления, отвечающий за отображение данных на странице;
- слой данных, отвечающий за изменение и хранение данных;
- презентер, отвечающий за связь двух слоев.

### Базовый код

#### Класс Api
Содержит в себе базовую логику отправки запросов. В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.
Методы: 
- `get` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер
- `post` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.

#### Класс EventEmitter
Брокер событий позволяет отправлять события и подписываться на события, происходящие в системе. Класс используется в презентере для обработки событий и в слоях приложения для генерации событий.  
Основные методы, реализуемые классом описаны интерфейсом `IEvents`:
- `on` - подписка на событие
- `emit` - инициализация события
- `trigger` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие   

#### Класс Component
Содержиит конструктор, принимающий основной HTML-элемент, который будет заполняться в дочерних классах. Так же класс предоставляет набор методов для взаимодействия с DOM.
- toggleClass(element: string, className: string):void - переключает CSS-класс для элемента
- setText(element: string, value: string):void - устанавливает текстовое содержимое элемента
- setDisabled(element: string, state: string):void - устанавливает или снимает атрибут disabled у элемента.
- setHidden(element: string):void и setVisible(element: string):void - скрывают или показывают элемент путем манипуляции стилем display
- setImage(element: string, src: string, alt?: string):void - устанавливает источник изображения и альтернативный текст для элемента типа HTMLImageElement.
- render(data?: string):void - метод для рендеринга компонента. Он принимает объект data, частично соответствующий типу T, и обновляет свойства экземпляра класса на основе переданных данных, а затем возвращает корневой элемент (container).

### Слой данных

#### Класс Cards
Класс отвечает за хранение и логику работы с данными карточек.\
Конструктор класса принимает инстант брокера событий.\
В полях класса хранятся следующие данные:
- _cards: ICard[] - массив объектов карточек
- _preview: string | null - id карточки, выбранной для просмотра в модальной окне
- events: IEvents - экземпляр класса `EventEmitter` для инициации событий при изменении данных.

Так же класс предоставляет набор методов для взаимодействия с этими данными.
- getCard(cardId: string): ICard - возвращает карточку по ее id
- а так-же сеттеры и геттеры для сохранения и получения данных из полей класса

#### Класс Basket
Класс отвечает за хранение и логику работы с данными карточек в корзине.\
Конструктор класса принимает инстант брокера событий.\
В полях класса хранятся следующие данные:
- _chosencards: ICard[] - массив объектов карточек
- events: IEvents - экземпляр класса `EventEmitter` для инициации событий при изменении данных.

Так же класс предоставляет набор методов для взаимодействия с этими данными.
- addCard(card: ICard): void - добавляет одну карточку в начало массива
- deleteCard(cardId: string, payload: Function | null): void - удаляет карточку из массива. Если передан колбэк, то выполняет его после удаления, если нет, то вызывает событие изменения массива
- clearBasket():void - удаляет все карточки из массива
- а так-же сеттер и геттер для сохранения и получения данных из полей класса

#### Класс CustomerInfo
Класс отвечает за хранение и логику работы с данными пользователя.\
Конструктор класса принимает инстант брокера событий.\
В полях класса хранятся следующие данные:
- _payment: TCustomerInfoPayment - данные пользователя об оплате
- _contacts: TCustomerInfoContacts - данные пользователя о контактах
- events: IEvents - экземпляр класса `EventEmitter` для инициации событий при изменении данных.

Так же класс предоставляет набор методов для взаимодействия с этими данными.
- checkValidation(data: Record<keyof TCustomerInfoPayment,  string> | Record<keyof TCustomerInfoContacts, string>):boolean - проверяет объект с данными карточки на валидность
- setPaymentInfo(paymentData: TCustomerInfoPayment): void - сохраняет данные об оплате в классе
- setContactInfo(contactData: TCustomerInfoContacts): void - сохраняет данные о контактах в классе
- clearData():void - удаляет данные пользователя

### Слой представления
Все классы представления отвечают за отображение внутри контейнера (DOM-элемент) передаваемых в них данных.

#### Класс Header
Отвечает за отображение шапки на главной страницы. Предоставляет метод `openBasket` для установки слушателя на клик на кнопку корзины для открытия ее модального окна. В конструктор принимает контейнер, в котором размещается кнопка корзины и счетчик товаров в корзине. Также класс предоставляет метод сеттера для установки значения количества товаров в корзине. 

Поля класса
- basket: HTMLElement - кнопка корзины
- basketCounter: HTMLElement - счетчик товаров корзины
- headerLogoImage: HTMLElement - логотип сайта

#### Класс CardsContainer
Отвечает за отображение блока с карточками на главной странице.  В конструктор принимает контейнер, в котором размещаются карточки.
Поля класса
-gallery: HTMLElement - коллекция карточек

#### Класс Card
Отвечает за отображение карточки, задавая в карточке данные названия, изображения, описания, цены, категории. Класс используется для отображения карточек на странице сайта, в модальном окне и в корзине. В конструктор класса передается DOM элемент темплейта, что позволяет при необходимости формировать карточки разных вариантов верстки. В классе устанавливаются слушатель на карточку, в результате взаимодействия с которыми пользователя генерируются соответствующие события.\
Поля класса содержат элементы разметки элементов карточки. Конструктор, кроме темплейта принимает экземпляр `EventEmitter` для инициации событий.\
Поля класса
-templateType: HTMLElement - темплейт разной отрисовки карточки
-cardImage: HTMLElement - рисунок карточки
-cardCategory: HTMLElement - категория карточки
-cardTitle: HTMLElement - название карточки
-cardText: HTMLElement - описание карточки
-cardPrice: HTMLElement - цена карточки
-cardButton: HTMLElement - кнопка карточки

Методы:
- setData(cardData: ICard): void - заполняет атрибуты элементов карточки данными.
- render(): HTMLElement - метод возвращает полностью заполненную карточку с установленными слушателями
- геттер id возвращает уникальный id карточки

#### Класс Modal
Реализует модальное окно. Также предоставляет методы `open` и `close` для управления отображением модального окна. Устанавливает слушатели на клавиатуру, для закрытия модального окна по Esc, на клик в оверлей и кнопку-крестик для закрытия попапа. Класс предоставляет сеттер для реализации разного контента в окне.
- constructor(selector: string, events: IEvents) Конструктор принимает селектор, по которому в разметке страницы будет идентифицировано стандартное модальное окно без контента и экземпляр класса `EventEmitter` для возможности инициации событий.

Поля класса
- content: HTMLElement - контент модального окна
- events: IEvents - брокер событий

#### Класс Basket
Отвечает за отображение корзины. В конструктор принимает контейнер с листом карточек, кнопкой и суммарной ценой покупки и экземпляр `EventEmitter` для инициации событий при нажатии пользователем на кнопки. Устанавливает в конструкторе слушатели на все кнопки, при срабатывании которых генерируются соответствующие события - удаление товара и переход к оформлению\

Методы: 

- updateTotal(total: number):number - Обновляет общую цену в корзине
- setValid(isValid: boolean): void - изменяет активность кнопки подтверждения при отсутствии товаров в корзине

Поля класса
- list: HTMLElement - элементы списка
- total: HTMLElement - суммарная цена
- button: HTMLElement - кнопка перехода к оформлению
- events: IEvents - брокер событий

#### Класс Form 
Отвечает за отображение формы содержащей поля ввода. При сабмите инициирует событие передавая в него объект с данными из полей ввода формы. При изменении данных в полях ввода инициирует событие изменения данных. Предоставляет методы для отображения ошибок и управления активностью кнопки сохранения.
Поля класса:
- submitButton: HTMLButtonElement - Кнопка подтверждения
- _form: HTMLFormElement - элемент формы
- formName: string - значение атрибута name формы
- inputs: NodeListOf<HTMLInputElement> - коллекция всех полей ввода формы
- errors: Record<string, HTMLElement> - объект хранящий все элементы для вывода ошибок под полями формы с привязкой к атрибуту name инпутов

Методы:
- setValid(isValid: boolean): void - изменяет активность кнопки подтверждения
- setInputValues(data: Record<string, string>): void - принимает объект с данными для заполнения полей формы
- setError(data: { field: string, value: string, validInformation: string }): void - принимает объект с данными для отображения или сокрытия текстов ошибок под полями ввода
- showInputError (field: string, errorMessage: string): void - отображает полученный текст ошибки под указанным полем ввода
- hideInputError (field: string): void - очищает текст ошибки под указанным полем ввода
- close (): void - расширяет родительский метод дополнительно при закрытии очищая поля формы и деактивируя кнопку сохранения
- getForm: HTMLElement - геттер для получения элемента формы

Поля класса
- formInput: HTMLElement - поля формы
- orderButtons: HTMLElement - кнопки выбора оплаты
- nextButton: HTMLElement - кнопка перехода далее

#### Класс Success
Отвечает за отображение окна об успешной покупке. В конструктор принимает контейнер с уведомлением об успешной операции и кнопкой подтверждения. В классе устанавливается слушатель на кнопку о переходе к главной странцие.

Поля класса
- total: HTMLElement - сумма покупки
- nextButton: HTMLElement - кнопка перехода далее

### Слой коммуникации

#### Класс AppApi
Принимает в конструктор экземпляр класса Api и предоставляет методы реализующие взаимодействие с бэкендом сервиса.

## Взаимодействие компонентов
Код, описывающий взаимодействие представления и данных между собой находится в файле `index.ts`, выполняющем роль презентера.\
Взаимодействие осуществляется за счет событий генерируемых с помощью брокера событий и обработчиков этих событий, описанных в `index.ts`\
В `index.ts` сначала создаются экземпляры всех необходимых классов, а затем настраивается обработка событий.

*Список всех событий, которые могут генерироваться в системе:*\
*События изменения данных (генерируются классами моделями данных)*
- `cards:changed` - изменение массива карточек на странице
- `card:selected` - изменение открываемой в модальном окне карточки
- `card:previewClear` - необходима очистка данных выбранной для показа в модальном окне карточки
- `chosenCards:changed` - изменение массива карточек в корзине
- `chosenCards:clear` - очистка данных в корзине
- `customerData:changed` - изменение данных пользователя 
- `customerData:clear` - очистка данных пользователя 

*События, возникающие при взаимодействии пользователя с интерфейсом (генерируются классами, отвечающими за представление)*
- `card:select` - выбор карточки для отображения в модальном окне
- `card:open` - открытие модального окна карточки
- `card:chose` - добавление карточки в корзину
- `basket:open` - открытие модального окна корзины
- `paymentEdit:open` - открытие модального окна с формой заполнения данных пользователя об оплате
- `contactsEdit:open` - открытие модального окна с формой заполнения данных пользователя о контактах
- `success:open` - открытие модального окна с успешной оплатой
- `payment:chose` - изменение данных о способе оплаты пользователя
- `address:input` - изменение данных в форме с адресом пользователя
- `email:input` - изменение данных в форме с email пользователя
- `phone:input` - изменение данных в форме с телефоном пользователя
- `payment:submit` - сохранение данных о способе оплаты и адресом пользователя
- `contacts:submit` - сохранение данных в форме с email и пользователя
- `address:validation` - событие, сообщающее о необходимости валидации данных в форме с адресом пользователя
- `email:validation` - событие, сообщающее о необходимости валидации данных в форме с email пользователя
- `phone:validation` - событие, сообщающее о необходимости валидации данных в форме с телефоном пользователя
